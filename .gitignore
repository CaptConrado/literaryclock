build/
.DS_Store
# The generated database is committed so the sdcard folder can be copied as-is.
literaryclock/build/
# Real card config holds WiFi credentials; only the example is tracked.
sdcard/literaryclock/config.txt
data/gutenberg/
