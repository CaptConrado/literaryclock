# Literary Clock settings. Edit on a computer or via the WiFi setup screen.

# WiFi network used to fetch the time. Leave blank to set the time by touch.
wifi_ssid = 
wifi_password = 

# Timezone: a name such as Europe/London or America/New_York (see README
# for the supported list), or a POSIX TZ string like GMT0BST,M3.5.0/1,M10.5.0
timezone = Europe/London

# orientation: landscape, portrait, landscape_flipped or portrait_flipped
orientation = landscape

clock_24h = true
show_time = true
sfw_only = false

# Backlight 5-255, and optional dimming between two times of day.
brightness = 255
night_brightness = 30
night_start = off
night_end = off

# Touch calibration is written here automatically, one touch_cal_N line per
# display rotation (swap,xmin,xmax,ymin,ymax). Delete a line to recalibrate.
